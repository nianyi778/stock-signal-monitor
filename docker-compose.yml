services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: stock-signal-monitor:1.0.0
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
